<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AddTaskActivity" >

    <EditText
        android:id="@+id/task_category_edittext"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="@string/hint_category"
        android:inputType="textCapWords" />

    <EditText
        android:id="@+id/task_summary_edittext"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/task_category_edittext"
        android:hint="@string/hint_summary"
        android:inputType="textCapSentences" />

    <EditText
        android:id="@+id/task_description_edittext"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/task_summary_edittext"
        android:hint="@string/hint_description"
        android:inputType="textCapSentences" />

    <Button
        android:id="@+id/button_add"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/task_description_edittext"
        android:layout_marginTop="18dp"
        android:enabled="false"
        android:text="@string/text_add" />

</RelativeLayout>